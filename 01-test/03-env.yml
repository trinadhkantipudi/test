apiversion: v1
kind: Pod
metadata:
  name: 03-env
spec:
  type: ClusterIP
  selector:
    app: nginx
  ports:
    - protocol: TCP
    port: 80
    targetport: 80
  heads:
    OS: windows
  lows: Linux
